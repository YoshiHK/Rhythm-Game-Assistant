
"""Eligibility policy for Phase 7 (CI and governance only)."""

# Games explicitly excluded from recommendations and why.
# Used by CI checks only; runtime behavior should not import this.
EXPLICIT_EXCLUSIONS = {
    # 'game_id': 'reason',
}
