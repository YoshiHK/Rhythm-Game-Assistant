[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rhythm-ingestion"
version = "0.1.0"
description = "Unified Ingestion Manager for multi‑game rhythm game song database."
authors = [
    { name = "Your Name", email = "you@example.com" }
]
requires-python = ">=3.9"

dependencies = [
    "openpyxl>=3.0.10",
    "jsonschema>=4.17.0",
    "pydantic>=2.6.1"
]

[project.scripts]
ingest = "rhythm_ingestion.orchestrator:main"