{
  "version": 2,
  "description": "Waivers for Phase 4.5 per-string token parity check. Use sparingly and keep under budget.",
  "budget": {
    "max_total": 5
  },
  "match_rules": {
    "wildcards": "Use '*' to match any locale/template/string_path.",
    "paths": "template and string_path match CI output (template_relpath::string_path)."
  },
  "waivers": [
    {
      "locale": "ja-JP",
      "template": "templates/narrative_v3/difficulty/example.json",
      "string_path": "strings.default",
      "reason": "Example waiver: grammar requires duplicated placeholder."
    }
  ]
}
