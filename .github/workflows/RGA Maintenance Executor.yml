name: RGA Maintenance Executor

on:
  workflow_dispatch:
    inputs:
      executor_mode:
        description: >-
          Bot #2 executor mode.

          plan:
          Generate an implementation plan from Bot #1 pre-audit evidence.

          dry_run_execute:
          Simulate execution and generate execution evidence without
          mutating repository state.

          execute:
          Apply gated execution after Bot #1 plan-audit and human approval.
        required: false
        default: "plan"
        type: choice
        options:
          - plan
          - dry_run_execute
          - execute

      audit_session_id:
        description: >-
          Optional lifecycle correlation id linking:
          Bot #1 pre-audit,
          Bot #2 implementation plan,
          Bot #1 plan-audit,
          human approval,
          Bot #2 execution,
          and Bot #1 post-audit.
        required: false
        default: ""

      pre_audit_report:
        description: >-
          Bot #1 pre-audit report path.

          This is the source material for Bot #2 implementation planning.
          Used when executor_mode=plan.
        required: false
        default: "artifacts/runtime_auditor_report.json"

      execution_plan:
        description: >-
          Bot #2 implementation / execution plan path.

          Used by dry_run_execute and execute stages.
        required: false
        default: "artifacts/execution_plan.json"

      approval_artifact:
        description: >-
          Human approval artifact path.

          Required before execute mode may apply gated execution.
        required: false
        default: "artifacts/human_execution_approval.json"

      execution_approval:
        description: >-
          Optional approval phrase for execute mode.

          Expected value:
          APPROVE_RGA_EXECUTION

          This does not replace the approval artifact; it is an additional
          safety signal for gated execution.
        required: false
        default: ""

jobs:
  maintenance-execution:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      actions: write
      pull-requests: write

    steps:
      ############################################################
      # Checkout Repository
      #
      # Token policy:
      #
      #   - Do not persist github.token credentials into git config.
      #
      #   - GitHub write operations in this workflow should use the
      #     generated GitHub App installation token.
      ############################################################

      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      ############################################################
      # Generate GitHub App Token
      #
      # Maintenance Executor lifecycle role:
      #
      #   - Bot #2 owns implementation planning and gated execution.
      #
      #   - Bot #2 may create approved tooling, workflow,
      #     governance, and execution evidence artifacts.
      #
      #   - Bot #2 must not approve, self-approve, merge, override
      #     governance, mutate runtime databases, delete source
      #     assets, or modify Completed Phases 1-7.
      #
      # Token policy:
      #
      #   - Prefer GitHub App installation token.
      #
      #   - Do not use PAT_TOKEN.
      #
      #   - Do not fall back to github.token.
      #
      #   - Do not print token values.
      ############################################################

      - name: Generate GitHub App Token
        id: github_app_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.RGA_APP_ID }}
          private-key: ${{ secrets.RGA_APP_PRIVATE_KEY }}
          
      ############################################################
      # Resolve GitHub App Artifact Token
      #
      # This exposes the generated GitHub App installation token
      # through one stable step output:
      #
      #   steps.artifact_token.outputs.token
      #
      # Downstream GitHub operations should use this output.
      ############################################################

      - name: Resolve GitHub App Artifact Token
        id: artifact_token
        shell: bash
        env:
          GENERATED_GITHUB_APP_TOKEN: ${{ steps.github_app_token.outputs.token }}
        run: |
          if [ -z "${GENERATED_GITHUB_APP_TOKEN}" ]; then
            echo "::error::Generated GitHub App token is empty."
            exit 1
          fi

          echo "token=${GENERATED_GITHUB_APP_TOKEN}" >> "$GITHUB_OUTPUT"                 

      ############################################################
      # Prepare Artifact Directory
      ############################################################

      - name: Prepare Artifact Directory
        shell: bash
        run: |
          mkdir -p artifacts

          python3 - <<'PY'
          import json
          from pathlib import Path

          payload = {
              "schema": "rga.maintenance_lifecycle_context.v1.0",
              "workflow": "RGA Maintenance Executor",
              "bot": "rga_executor_bot",
              "bot_role": "planner_executor",
              "lifecycle_model": [
                  "bot_1_pre_audit",
                  "bot_2_implementation_plan",
                  "bot_1_plan_audit",
                  "human_approval",
                  "bot_2_execute",
                  "bot_1_post_audit",
              ],
              "authority": {
                  "bot_2_may_plan": True,
                  "bot_2_may_execute": True,
                  "bot_2_may_approve": False,
                  "bot_2_may_self_approve": False,
                  "human_approval_required": True,
              },
              "phase_boundary_policy": {
                  "completed_phases_immutable": True,
                  "canonical_row_modification_allowed": False,
                  "pattern_logic_modification_allowed": False,
                  "tips_generation_modification_allowed": False,
                  "personalization_modification_allowed": False,
                  "localization_modification_allowed": False,
                  "recommendation_logic_modification_allowed": False,
              },
          }

          Path("artifacts/maintenance_lifecycle_context.json").write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY
          
      ############################################################
      # Ingest Pre-Audit Artifacts
      ############################################################          
          
      - name: Ingest Pre-Audit Artifacts
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: RGA Runtime Auditor.yml
          name: runtime-auditor-evidence.zip
          path: artifacts/
          
      ############################################################
      # Flatten Artifact Directory
      ############################################################          

      - name: Flatten Artifact Directory
        run: |
          # Move files inside subfolders up to artifacts/
          find artifacts/ -mindepth 2 -type f -exec mv {} artifacts/ \;
          echo "[RGA] Files in artifacts folder:"
          ls -la artifacts/          

      ############################################################
      # Write Database Mutation Contract
      #
      # Plan-stage artifact.
      #
      # Runtime Auditor consumes this artifact during:
      #
      #   plan_audit
      #
      # This artifact is a proposal contract only.
      #
      # Execute stage must not modify this artifact.
      ############################################################

      - name: Write Database Mutation Contract
        if: ${{ always() && inputs.executor_mode == 'plan' }}
        env:
          AUDIT_SESSION_ID: ${{ inputs.audit_session_id }}
          EXECUTOR_MODE: ${{ inputs.executor_mode }}
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path
          from datetime import datetime, timezone

          runtime_baseline = Path(
              "artifacts/runtime_baseline.json"
          )

          pre_audit_report = Path(
              "artifacts/runtime_auditor_report.json"
          )

          audit_session_id = os.environ.get(
              "AUDIT_SESSION_ID",
              "",
          )

          payload = {
              "schema":
                  "rga.database_mutation_plan.v1.1",

              "workflow":
                  "RGA Maintenance Executor",

              "bot":
                  "rga_executor_bot",

              "lifecycle_stage":
                  "bot_2_plan",

              "artifact_role":
                  "proposal_contract",

              "plan_stage_artifact":
                  True,

              "runtime_auditor_plan_audit_required":
                  True,

              "execution_stage_may_mutate_plan":
                  False,

              "approval_required":
                  True,

              "audit_session_id":
                  audit_session_id,

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "runtime_baseline": {
                  "path": str(runtime_baseline),
                  "present": runtime_baseline.exists(),
              },

              "pre_audit_report": {
                  "path": str(pre_audit_report),
                  "present": pre_audit_report.exists(),
              },

              "approval": {
                  "required": True,
                  "approved": False,
                  "approval_artifact":
                      "artifacts/human_execution_approval.json",
              },

              "policy": {
                  "validation_required": True,
                  "validated": False,
                  "phase_boundary_immutable": True,
              },

              "proposed_mutations": [],
          }

          Path(
              "artifacts/database_mutation_plan.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )

          print(
              "[RGA] database_mutation_plan.json generated successfully."
          )
          PY
          
      ############################################################
      # Write Persistence Contract
      #
      # Plan-stage governance contract.
      #
      # Consumed by:
      #
      #   Runtime Auditor plan-audit
      #   Deployment Governance Gate
      #
      # This contract does not authorize execution.
      ############################################################

      - name: Write Persistence Contract
        if: ${{ always() && inputs.executor_mode == 'plan' }}
        shell: python
        env:
          AUDIT_SESSION_ID: ${{ inputs.audit_session_id }}
        run: |
          import json
          import os
          from pathlib import Path
          from datetime import datetime, timezone

          mutation_plan = Path(
              "artifacts/database_mutation_plan.json"
          )

          audit_session_id = os.environ.get(
              "AUDIT_SESSION_ID",
              "",
          )

          payload = {
              "schema":
                  "rga.persistence_contract.v1.1",

              "workflow":
                  "RGA Maintenance Executor",

              "bot":
                  "rga_executor_bot",

              "lifecycle_stage":
                  "stage_4_persistence_contract",

              "artifact_role":
                  "governance_contract",

              "plan_stage_artifact":
                  True,

              "runtime_auditor_plan_audit_required":
                  True,

              "deployment_gate_validation_required":
                  True,

              "execution_authority":
                  False,

              "audit_session_id":
                  audit_session_id,

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "source_contract":
                  str(mutation_plan),

              "source_contract_present":
                  mutation_plan.exists(),

              "ownership": {
                  "planner": "bot_2",
                  "executor": "persistence_layer",
                  "approver": "human",
                  "policy_validator": "deployment_gate",
              },

              "permissions": {
                  "executor_may_write_db": False,
                  "persistence_layer_owns_db_writes": True,
                  "human_approval_required": True,
                  "deployment_gate_required": True,
              },

              "runtime_requirements": {
                  "runtime_baseline_required": True,
                  "mutation_validation_required": True,
              },

              "status": {
                  "approved": False,
                  "validated": False,
                  "executed": False,
              },
          }

          Path(
              "artifacts/persistence_contract.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )

          print(
              "[RGA] persistence_contract.json generated successfully."
          )

      ############################################################
      # Human Approval Artifact audit
      ############################################################

      - name: Human Approval Artifact audit
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
     
          p = Path('artifacts/human_execution_approval.json')

          payload = {
              'schema':'rga.approval_audit.v1.0',
              'artifact_present': p.exists()
          }
          Path('artifacts/approval_audit.json').write_text(
              json.dumps(payload, indent=2),
              encoding='utf-8'
          )
          PY
          
      ############################################################
      # Run Runtime Executor
      #
      # Bot #2 lifecycle role:
      #
      #   plan:
      #     Generate implementation plan from Bot #1 pre-audit evidence.
      #
      #   dry_run_execute:
      #     Simulate execution and produce execution evidence without
      #     mutating repository state.
      #
      #   execute:
      #     Apply gated execution only after Bot #1 plan-audit,
      #     approval artifact matching, execution gate, and human approval.
      #
      # Execution plan handling:
      #
      #   - artifacts/execution_plan.json is a plan-stage proposal artifact.
      #
      #   - It is preserved as an input contract for dry_run_execute
      #     and execute stages.
      #
      #   - Runtime Auditor plan-audit is the verifier of:
      #
      #       artifacts/execution_plan.json
      #       artifacts/database_mutation_plan.json
      #
      #   - Execute mode must not overwrite artifacts/execution_plan.json.
      #
      #   - The true execution signal is:
      #
      #       executor_mode=execute
      #       artifacts/human_execution_approval.json
      #       artifacts/approval_artifact_matching.json
      #       artifacts/execution_gate.json
      #
      #   - Do not pass --execution-plan unless runtime_executor.py
      #     explicitly supports that CLI argument.
      #
      # This step intentionally preserves:
      #
      #   execution_plan.json
      #   execution_plan_input_contract.json
      #   runtime_executor_output.json
      #   runtime_executor_report.json
      #   runtime_executor_report.md
      #   apply_execution_result.json
      #   executor_write_manifest.json
      #   execution_provenance.json
      #   runtime_executor_stdout.txt
      #   runtime_executor_stderr.txt
      #   runtime_executor_stderr_summary.json
      #   runtime_executor_exit_code.txt
      #
      # Empty stderr is not treated as a failure. It is summarized as
      # explicit zero-error evidence.
      ############################################################

      - name: Run Runtime Executor
        id: run_runtime_executor
        if: >-
          ${{
            always()
            && (
              inputs.executor_mode != 'execute'
              || steps.execution_gate.outputs.allowed == 'true'
            )
          }}
        continue-on-error: true
        shell: bash
        env:
          EXECUTOR_MODE: ${{ inputs.executor_mode }}
          EXECUTION_PLAN_IN: ${{ inputs.execution_plan }}
          PRE_AUDIT_REPORT_IN: ${{ inputs.pre_audit_report }}
        run: |
          set -uo pipefail

          mkdir -p artifacts

          MODE="${EXECUTOR_MODE:-dry_run_execute}"
          EXECUTION_PLAN="${EXECUTION_PLAN_IN:-artifacts/execution_plan.json}"
          PRE_AUDIT_REPORT="${PRE_AUDIT_REPORT_IN:-artifacts/runtime_auditor_report.json}"

          ########################################################
          # Preserve and validate execution_plan.json as input
          #
          # Important:
          #   - In plan mode, runtime_executor.py may write
          #     artifacts/execution_plan.json.
          #
          #   - In dry_run_execute / execute mode, execution_plan.json
          #     is treated as an input artifact and must not be
          #     overwritten by this step.
          ########################################################

          cat > artifacts/prepare_execution_plan_input_contract.py <<'PY'
          import hashlib
          import json
          import os
          from datetime import datetime, timezone
          from pathlib import Path

          mode = os.environ.get(
              "MODE",
              "dry_run_execute",
          )

          execution_plan_path = Path(
              os.environ.get(
                  "EXECUTION_PLAN",
                  "artifacts/execution_plan.json",
              )
          )

          out_path = Path(
              "artifacts/execution_plan_input_contract.json"
          )

          payload = {
              "schema":
                  "rga.execution_plan_input_contract.v1.0",

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "mode":
                  mode,

              "execution_plan_path":
                  str(execution_plan_path),

              "execution_plan_present":
                  execution_plan_path.exists(),

              "execution_plan_parseable":
                  False,

              "execution_plan_sha256":
                  None,

              "execution_plan_schema":
                  None,

              "execution_plan_mode":
                  None,

              "plan_stage_artifact":
                  True,

              "execute_stage_may_overwrite_execution_plan":
                  False,

              "runtime_executor_cli_argument_supported":
                  False,

              "runtime_executor_cli_argument":
                  "--execution-plan",

              "required_for_modes": [
                  "dry_run_execute",
                  "execute"
              ],

              "policy_violations":
                  [],
          }

          if execution_plan_path.exists():
              raw = execution_plan_path.read_bytes()

              payload[
                  "execution_plan_sha256"
              ] = hashlib.sha256(
                  raw
              ).hexdigest()

              try:
                  data = json.loads(
                      raw.decode(
                          "utf-8",
                      )
                  )

                  payload[
                      "execution_plan_parseable"
                  ] = True

                  payload[
                      "execution_plan_schema"
                  ] = data.get(
                      "schema",
                  )

                  payload[
                      "execution_plan_mode"
                  ] = data.get(
                      "mode",
                  )

              except Exception as exc:
                  payload[
                      "policy_violations"
                  ].append(
                      f"execution_plan.json is not parseable JSON: {exc}"
                  )

          else:
              if mode in {
                  "dry_run_execute",
                  "execute",
              }:
                  payload[
                      "policy_violations"
                  ].append(
                      "execution_plan.json is required as an input contract for this mode."
                  )

          out_path.write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY

          MODE="${MODE}" \
          EXECUTION_PLAN="${EXECUTION_PLAN}" \
          python artifacts/prepare_execution_plan_input_contract.py

          ########################################################
          # Select json-out path by mode
          #
          # plan:
          #   runtime_executor.py writes artifacts/execution_plan.json
          #
          # dry_run_execute / execute:
          #   preserve artifacts/execution_plan.json and write executor
          #   runtime output elsewhere.
          ########################################################

          if [ "${MODE}" = "plan" ]; then
            JSON_OUT="artifacts/execution_plan.json"
          else
            JSON_OUT="artifacts/runtime_executor_output.json"
          fi

          set +e

          ########################################################
          # Do NOT pass --execution-plan here.
          #
          # Current runtime_executor.py does not support that CLI
          # argument. execution_plan.json is preserved and described
          # by artifacts/execution_plan_input_contract.json instead.
          ########################################################

          python tools/runtime_executor.py \
            --mode "${MODE}" \
            --pre-audit-report "${PRE_AUDIT_REPORT}" \
            --json-out "${JSON_OUT}" \
            --report-json-out "artifacts/runtime_executor_report.json" \
            --md-out "artifacts/runtime_executor_report.md" \
            > "artifacts/runtime_executor_stdout.txt" \
            2> "artifacts/runtime_executor_stderr.txt"

          EXECUTOR_EXIT_CODE="$?"

          set -e

          echo "${EXECUTOR_EXIT_CODE}" > artifacts/runtime_executor_exit_code.txt

          ########################################################
          # Ensure stdout / stderr artifacts exist
          ########################################################

          if [ ! -f artifacts/runtime_executor_stdout.txt ]; then
            touch artifacts/runtime_executor_stdout.txt
          fi

          if [ ! -f artifacts/runtime_executor_stderr.txt ]; then
            touch artifacts/runtime_executor_stderr.txt
          fi

          ########################################################
          # Structured stderr summary
          ########################################################

          STDERR_PRESENT="false"
          STDERR_EMPTY="true"

          STDOUT_PRESENT="false"
          STDOUT_EMPTY="true"

          ERROR_MARKER_COUNT="0"
          WARNING_MARKER_COUNT="0"

          if [ -f artifacts/runtime_executor_stderr.txt ]; then
            STDERR_PRESENT="true"

            if [ -s artifacts/runtime_executor_stderr.txt ]; then
              STDERR_EMPTY="false"
            fi
          fi

          if [ -f artifacts/runtime_executor_stdout.txt ]; then
            STDOUT_PRESENT="true"

            if [ -s artifacts/runtime_executor_stdout.txt ]; then
              STDOUT_EMPTY="false"
            fi
          fi

          ERROR_MARKER_COUNT="$(
            {
              grep -E \
                'Traceback|Error|Exception|ModuleNotFoundError|RuntimeError|ValueError|NameError|TypeError|SyntaxError|unrecognized arguments' \
                artifacts/runtime_executor_stderr.txt \
                2>/dev/null \
                || true
            } \
              | wc -l \
              | tr -d ' '
          )"

          WARNING_MARKER_COUNT="$(
            {
              grep -E \
                'warning|Warning|WARN' \
                artifacts/runtime_executor_stderr.txt \
                2>/dev/null \
                || true
            } \
              | wc -l \
              | tr -d ' '
          )"

          EXEC_FAILED="false"
          if [ "${EXECUTOR_EXIT_CODE}" != "0" ]; then
            EXEC_FAILED="true"
          fi

          jq -n \
            --argjson stderr_present ${STDERR_PRESENT} \
            --argjson stderr_empty ${STDERR_EMPTY} \
            --argjson stdout_present ${STDOUT_PRESENT} \
            --argjson stdout_empty ${STDOUT_EMPTY} \
            --argjson exit_code ${EXECUTOR_EXIT_CODE} \
            --argjson failed ${EXEC_FAILED} \
            --argjson err_count ${ERROR_MARKER_COUNT} \
            --argjson warn_count ${WARNING_MARKER_COUNT} \
            '{
              schema: "rga.runtime_executor.stderr_summary.v1.0",
              stderr_present: $stderr_present,
              stderr_empty: $stderr_empty,
              stdout_present: $stdout_present,
              stdout_empty: $stdout_empty,
              executor_exit_code: $exit_code,
              execution_failed: $failed,
              error_marker_count: $err_count,
              warning_marker_count: $warn_count
            }' > artifacts/runtime_executor_stderr_summary.json

          ########################################################
          # Fallback runtime_executor_report.json
          ########################################################

          if [ ! -s artifacts/runtime_executor_report.json ]; then

            jq -n \
              --arg mode "${MODE}" \
              --arg json_out "${JSON_OUT}" \
              --arg execution_plan "${EXECUTION_PLAN}" \
              --argjson exit_code ${EXECUTOR_EXIT_CODE} \
              --argjson stdout_present ${STDOUT_PRESENT} \
              --argjson stderr_present ${STDERR_PRESENT} \
              --argjson stdout_empty ${STDOUT_EMPTY} \
              --argjson stderr_empty ${STDERR_EMPTY} \
              '{
                schema: "rga.runtime_executor.report.fallback.v1.0",
                mode: $mode,
                proposal_only: ($mode == "plan"),
                dry_run: ($mode == "dry_run_execute"),
                execution_authority: ($mode == "execute"),
                approval_authority: false,
                plan: {},
                dry_run_result: {},
                apply_execution_result: {},
                executor_write_manifest: {},
                execution_provenance: {},
                execution_plan_input: {
                  path: $execution_plan,
                  contract: "artifacts/execution_plan_input_contract.json",
                  plan_stage_artifact: true,
                  execute_stage_may_overwrite_execution_plan: false
                },
                runtime_output: {
                  json_out: $json_out
                },
                diagnostics: {
                  fallback_report_generated: true,
                  executor_exit_code: $exit_code,
                  stdout_present: $stdout_present,
                  stderr_present: $stderr_present,
                  stdout_empty: $stdout_empty,
                  stderr_empty: $stderr_empty,
                  policy_result: {
                    policy_passed: false,
                    violations: [
                      "runtime_executor.py did not emit runtime_executor_report.json."
                    ]
                  }
                }
              }' > artifacts/runtime_executor_report.json

          fi

          ########################################################
          # Fallback runtime_executor_report.md
          ########################################################

          if [ ! -s artifacts/runtime_executor_report.md ]; then
            cat > artifacts/runtime_executor_report.md <<'MD'
          # RGA Runtime Executor Report

          ## Fallback Report

          `runtime_executor.py` did not emit a non-empty Markdown report.

          See:

          - `runtime_executor_report.json`
          - `runtime_executor_stdout.txt`
          - `runtime_executor_stderr.txt`
          - `runtime_executor_stderr_summary.json`
          - `runtime_executor_exit_code.txt`
          - `execution_plan_input_contract.json`
          MD
          fi

          echo "Runtime executor exit code: ${EXECUTOR_EXIT_CODE}"
          echo "Runtime executor evidence artifacts generated."
          echo "Execution plan input contract: artifacts/execution_plan_input_contract.json"

          exit 0
          

      ############################################################
      # Execution Plan Fingerprint
      #
      # execution_plan.json is a plan-stage proposal artifact.
      #
      # Runtime Auditor plan-audit reviews:
      #
      #   - execution_plan.json
      #   - database_mutation_plan.json
      #
      # Human approval is expected to approve the reviewed plan.
      #
      # Execute stage must preserve the approved plan and should
      # verify that the approved artifact has not changed.
      ############################################################

      - name: Execution Plan Fingerprint
        if: always()
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p artifacts

          python - <<'PY'
          import hashlib
          import json
          from pathlib import Path
          from datetime import datetime, timezone

          plan_path = Path(
              "artifacts/execution_plan.json"
          )

          mutation_plan_path = Path(
              "artifacts/database_mutation_plan.json"
          )

          fingerprint = None
          plan_schema = None
          plan_mode = None
          plan_parseable = False

          if plan_path.exists() and plan_path.is_file():

              raw = plan_path.read_bytes()

              fingerprint = hashlib.sha256(
                  raw
              ).hexdigest()

              try:
                  plan_data = json.loads(
                      raw.decode(
                          "utf-8"
                      )
                  )

                  plan_parseable = True

                  plan_schema = plan_data.get(
                      "schema"
                  )

                  plan_mode = plan_data.get(
                      "mode"
                  )

              except Exception:
                  pass

          mutation_fingerprint = None

          if (
              mutation_plan_path.exists()
              and mutation_plan_path.is_file()
          ):
              mutation_fingerprint = hashlib.sha256(
                  mutation_plan_path.read_bytes()
              ).hexdigest()

          payload = {
              "schema":
                  "rga.execution_plan_fingerprint.v1.1",

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "artifact_role":
                  "plan_fingerprint",

              "plan_stage_artifact":
                  True,

              "runtime_auditor_plan_audit_required":
                  True,

              "human_approval_required":
                  True,

              "execution_stage_may_modify_plan":
                  False,

              "execution_stage_may_modify_mutation_plan":
                  False,

              "execution_plan": {
                  "path":
                      str(plan_path),

                  "present":
                      plan_path.exists()
                      and plan_path.is_file(),

                  "parseable":
                      plan_parseable,

                  "schema":
                      plan_schema,

                  "mode":
                      plan_mode,

                  "sha256":
                      fingerprint,
              },

              "database_mutation_plan": {
                  "path":
                      str(mutation_plan_path),

                  "present":
                      mutation_plan_path.exists()
                      and mutation_plan_path.is_file(),

                  "sha256":
                      mutation_fingerprint,
              },

              "approval_matching_contract": {
                  "artifact":
                      "artifacts/approval_artifact_matching.json",

                  "expected_inputs": [
                      "execution_plan.json",
                      "database_mutation_plan.json",
                  ],
              },

              "lifecycle_sequence": [
                  "bot_2_plan",
                  "bot_1_plan_audit",
                  "human_approval",
                  "bot_2_execute",
                  "bot_1_post_audit",
              ],
          }

          Path(
              "artifacts/execution_plan_fingerprint.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY
          
      ############################################################
      # Execution Plan Input Contract
      #
      # Protects the approved plan-stage artifact from being
      # regenerated or overwritten during:
      #
      #   dry_run_execute
      #   execute
      #
      # This is a contract artifact.
      #
      # It does NOT grant execution authority.
      ############################################################

      - name: Execution Plan Input Contract
        if: always()
        shell: bash
        env:
          EXECUTOR_MODE: ${{ inputs.executor_mode }}
        run: |
          set -euo pipefail

          mkdir -p artifacts

          cat > artifacts/build_execution_plan_input_contract.py <<'PY'
          import hashlib
          import json
          import os
          from datetime import datetime, timezone
          from pathlib import Path

          plan_file = Path(
              "artifacts/execution_plan.json"
          )

          mode = (
              os.environ.get(
                  "EXECUTOR_MODE",
                  "plan",
              ).strip()
              or "plan"
          )

          payload = {
              "schema":
                  "rga.execution_plan_input_contract.v1.0",

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "mode":
                  mode,

              "execution_plan_path":
                  str(plan_file),

              "execution_plan_present":
                  False,

              "execution_plan_parseable":
                  False,

              "execution_plan_sha256":
                  None,

              "execution_plan_schema":
                  None,

              "execution_plan_mode":
                  None,

              "plan_stage_artifact":
                  True,

              "execute_stage_may_overwrite_execution_plan":
                  False,

              "runtime_executor_cli_argument_supported":
                  False,

              "runtime_executor_cli_argument":
                  "--execution-plan",

              "required_for_modes": [
                  "dry_run_execute",
                  "execute",
              ],

              "policy_violations": [],
          }

          if not plan_file.exists():
              payload[
                  "policy_violations"
              ].append(
                  "execution_plan.json is missing."
              )

          else:
              payload[
                  "execution_plan_present"
              ] = True

              try:
                  raw = plan_file.read_bytes()

                  payload[
                      "execution_plan_sha256"
                  ] = hashlib.sha256(
                      raw
                  ).hexdigest()

                  plan = json.loads(
                      raw.decode(
                          "utf-8"
                      )
                  )

                  payload[
                      "execution_plan_parseable"
                  ] = True

                  payload[
                      "execution_plan_schema"
                  ] = plan.get(
                      "schema"
                  )

                  payload[
                      "execution_plan_mode"
                  ] = plan.get(
                      "mode"
                  )

                  if (
                      plan.get("mode")
                      != "plan"
                  ):
                      payload[
                          "policy_violations"
                      ].append(
                          "execution_plan.json must remain a plan-stage artifact with mode=plan."
                      )

              except Exception as exc:
                  payload[
                      "policy_violations"
                  ].append(
                      f"Could not parse execution_plan.json: {exc}"
                  )

          Path(
              "artifacts/execution_plan_input_contract.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY

          python3 artifacts/build_execution_plan_input_contract.py

          rm -f artifacts/build_execution_plan_input_contract          
          
      ############################################################
      # Approval Artifact Matching
      #
      # Verifies:
      #
      #   human_execution_approval.json
      #       ↓
      #   execution_plan_fingerprint.json
      #
      # using the approval bundle fingerprints.
      #
      # This step does NOT approve execution.
      # It only verifies that the approved bundle matches
      # the generated proposal artifacts.
      ############################################################

      - name: Approval Artifact Matching
        if: always()
        shell: bash
        run: |
          set -euo pipefail

          python3 <<'PY'
          import json
          from pathlib import Path

          fingerprint_file = Path(
              "artifacts/execution_plan_fingerprint.json"
          )

          approval_file = Path(
              "artifacts/human_execution_approval.json"
          )

          plan_file = Path(
              "artifacts/execution_plan.json"
          )

          mutation_file = Path(
              "artifacts/database_mutation_plan.json"
          )

          approval_present = approval_file.exists()
          fingerprint_present = fingerprint_file.exists()

          approval_matches_plan = False
          approval_matches_mutation_plan = False

          violations = []

          approval = {}
          fingerprint = {}

          if approval_present:
              try:
                  approval = json.loads(
                      approval_file.read_text(
                          encoding="utf-8"
                      )
                  )
              except Exception as exc:
                  violations.append(
                      f"Could not parse approval artifact: {exc}"
                  )

          if fingerprint_present:
              try:
                  fingerprint = json.loads(
                      fingerprint_file.read_text(
                          encoding="utf-8"
                      )
                  )
              except Exception as exc:
                  violations.append(
                      f"Could not parse execution plan fingerprint: {exc}"
                  )

          #
          # ------------------------------------------------------
          # v2.1 approval bundle
          # ------------------------------------------------------
          #

          approved_bundle = approval.get(
              "approved_execution_bundle",
              {},
          )

          approved_plan_sha = (
              approved_bundle.get(
                  "execution_plan_sha256"
              )
          )

          approved_mutation_sha = (
              approved_bundle.get(
                  "database_mutation_plan_sha256"
              )
          )

          #
          # ------------------------------------------------------
          # Fingerprint artifact
          # ------------------------------------------------------
          #

          plan_sha = (
              fingerprint.get(
                  "execution_plan",
                  {},
              ).get(
                  "sha256"
              )
          )

          mutation_sha = (
              fingerprint.get(
                  "database_mutation_plan",
                  {},
              ).get(
                  "sha256"
              )
          )

          if (
              approved_plan_sha
              and plan_sha
          ):
              approval_matches_plan = (
                  approved_plan_sha
                  == plan_sha
              )

          if (
              approved_mutation_sha
              and mutation_sha
          ):
              approval_matches_mutation_plan = (
                  approved_mutation_sha
                  == mutation_sha
              )

          #
          # ------------------------------------------------------
          # Validation messages
          # ------------------------------------------------------
          #

          if not approval_present:
              violations.append(
                  "human_execution_approval.json is missing."
              )

          if not fingerprint_present:
              violations.append(
                  "execution_plan_fingerprint.json is missing."
              )

          if (
              approval_present
              and not approved_plan_sha
          ):
              violations.append(
                  "Approval bundle is missing execution_plan_sha256."
              )

          if (
              approval_present
              and not approved_mutation_sha
          ):
              violations.append(
                  "Approval bundle is missing database_mutation_plan_sha256."
              )

          if (
              approved_plan_sha
              and plan_sha
              and not approval_matches_plan
          ):
              violations.append(
                  "Approval bundle execution_plan_sha256 does not match execution_plan_fingerprint."
              )

          if (
              approved_mutation_sha
              and mutation_sha
              and not approval_matches_mutation_plan
          ):
              violations.append(
                  "Approval bundle database_mutation_plan_sha256 does not match execution_plan_fingerprint."
              )

          payload = {
              "schema":
                  "rga.approval_artifact_matching.v1.2",

              "approval_artifact_present":
                  approval_present,

              "fingerprint_artifact_present":
                  fingerprint_present,

              "execution_plan_present":
                  plan_file.exists(),

              "database_mutation_plan_present":
                  mutation_file.exists(),

              "approved_execution_plan_sha256":
                  approved_plan_sha,

              "fingerprint_execution_plan_sha256":
                  plan_sha,

              "approved_database_mutation_plan_sha256":
                  approved_mutation_sha,

              "fingerprint_database_mutation_plan_sha256":
                  mutation_sha,

              "approval_matches_execution_plan":
                  approval_matches_plan,

              "approval_matches_database_mutation_plan":
                  approval_matches_mutation_plan,

              "all_required_matches_passed":
                  (
                      approval_matches_plan
                      and
                      approval_matches_mutation_plan
                  ),

              "policy_violations":
                  violations,
          }

          Path(
              "artifacts/approval_artifact_matching.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY
          
      ############################################################
      # Execution Scope audit
      ############################################################

      - name: Execution Scope audit
        if: always()
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          payload = {
              "schema": "rga.execution_scope_audit.v1.0",
              "protected_scopes": [
                  "canonical_row",
                  "pattern_logic",
                  "tips_generation",
                  "personalization",
                  "localization",
                  "recommendation_logic",
              ],
              "completed_phases_immutable": True,
          }
          Path("artifacts/execution_scope_audit.json").write_text(
              json.dumps(payload, indent=2),
              encoding="utf-8",
          )
          PY

      ############################################################
      # Execution Policy audit
      ############################################################

      - name: Execution Policy audit
        if: always()
        shell: bash   
        env:
          EXECUTOR_MODE: ${{ inputs.executor_mode }}
        run: |
          set -euo pipefail
          python3 <<'PY'
          import json
          import os
          from pathlib import Path
          mode = os.environ["EXECUTOR_MODE"]
          payload = {
              "schema": "rga.execution_policy_audit.v1.0",
              "execution_authority": mode == "execute",
              "approval_authority": False,
              "self_approval": False,
              "human_approval_required": True,
              "plan_audit_required": True,
              "post_audit_required": True,
              "mode": mode,
          }
          Path(
              "artifacts/execution_policy_audit.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
              ),
              encoding="utf-8",
          )
          PY
          
      ############################################################
      # Execution Gate
      #
      # Final workflow-side admission gate for Bot #2 execution.
      #
      # plan:
      #   Planning mode is allowed without execution approval.
      #
      # dry_run_execute:
      #   Non-mutating simulation is allowed when the execution plan
      #   input contract is present and valid.
      #
      # execute:
      #   Live execution is allowed only when:
      #
      #     - Runtime Auditor pre-audit evidence exists.
      #     - The plan-stage execution plan exists and is parseable.
      #     - The execution plan input contract has no violations.
      #     - Human approval exists and is structurally valid.
      #     - The approval phrase is correct.
      #     - Human review and Bot #1 plan-audit are complete.
      #     - Approval fingerprints match the plan bundle.
      #     - Completed Phases remain immutable.
      #
      # This gate does not approve execution.
      # Human approval remains the approval authority.
      #
      # Canonical output:
      #
      #   artifacts/execution_gate.json
      #
      # Step outputs:
      #
      #   status
      #   allowed
      #   reason
      ############################################################

      - name: Execution Gate
        id: execution_gate
        if: always()
        shell: bash
        env:
          EXECUTOR_MODE: ${{ inputs.executor_mode }}
          APPROVAL_ARTIFACT: ${{ inputs.approval_artifact }}
          PRE_AUDIT_REPORT: ${{ inputs.pre_audit_report }}
          REQUIRED_APPROVAL_PHRASE: APPROVE_RGA_EXECUTION
        run: |
          set -euo pipefail

          mkdir -p artifacts

          cat > artifacts/evaluate_execution_gate.py <<'PY'
          import json
          import os
          from datetime import datetime, timezone
          from pathlib import Path
          from typing import Any, Dict, List, Tuple

          mode = (
              os.environ.get(
                  "EXECUTOR_MODE",
                  "plan",
              ).strip()
              or "plan"
          )

          approval_path = Path(
              os.environ.get(
                  "APPROVAL_ARTIFACT",
                  "artifacts/human_execution_approval.json",
              ).strip()
              or "artifacts/human_execution_approval.json"
          )

          pre_audit_path = Path(
              os.environ.get(
                  "PRE_AUDIT_REPORT",
                  "artifacts/runtime_auditor_report.json",
              ).strip()
              or "artifacts/runtime_auditor_report.json"
          )

          required_approval_phrase = os.environ.get(
              "REQUIRED_APPROVAL_PHRASE",
              "APPROVE_RGA_EXECUTION",
          )

          execution_plan_path = Path(
              "artifacts/execution_plan.json"
          )

          mutation_plan_path = Path(
              "artifacts/database_mutation_plan.json"
          )

          persistence_contract_path = Path(
              "artifacts/persistence_contract.json"
          )

          fingerprint_path = Path(
              "artifacts/execution_plan_fingerprint.json"
          )

          input_contract_path = Path(
              "artifacts/execution_plan_input_contract.json"
          )

          approval_matching_path = Path(
              "artifacts/approval_artifact_matching.json"
          )

          plan_audit_path = Path(
              "artifacts/plan_audit_evidence.json"
          )

          execution_policy_path = Path(
              "artifacts/execution_policy_audit.json"
          )

          output_path = Path(
              "artifacts/execution_gate.json"
          )

          def load_json(
              path: Path,
          ) -> Tuple[Dict[str, Any], str | None]:
              if not path.exists():
                  return (
                      {},
                      f"{path} is missing.",
                  )

              if not path.is_file():
                  return (
                      {},
                      f"{path} is not a file.",
                  )

              if path.stat().st_size == 0:
                  return (
                      {},
                      f"{path} is empty.",
                  )

              try:
                  value = json.loads(
                      path.read_text(
                          encoding="utf-8",
                      )
                  )

                  if not isinstance(
                      value,
                      dict,
                  ):
                      return (
                          {},
                          f"{path} is not a JSON object.",
                      )

                  return (
                      value,
                      None,
                  )

              except Exception as exc:
                  return (
                      {},
                      f"Could not parse {path}: {exc}",
                  )

          checks: Dict[str, Any] = {}
          violations: List[str] = []
          warnings: List[str] = []

          supported_modes = {
              "plan",
              "dry_run_execute",
              "execute",
          }

          mode_supported = mode in supported_modes

          checks[
              "mode_supported"
          ] = mode_supported

          if not mode_supported:
              violations.append(
                  f"Unsupported executor mode: {mode}"
              )

          #
          # ------------------------------------------------------
          # Plan mode
          # ------------------------------------------------------
          #

          if mode == "plan":
              status = (
                  "allowed"
                  if not violations
                  else "blocked"
              )

              reason = (
                  "Plan mode accepted."
                  if status == "allowed"
                  else "Plan mode blocked by gate violations."
              )

          #
          # ------------------------------------------------------
          # Dry-run / execute shared plan-input checks
          # ------------------------------------------------------
          #

          else:
              execution_plan, execution_plan_error = (
                  load_json(
                      execution_plan_path
                  )
              )

              input_contract, input_contract_error = (
                  load_json(
                      input_contract_path
                  )
              )

              checks[
                  "execution_plan_present"
              ] = execution_plan_error is None

              checks[
                  "execution_plan_parseable"
              ] = execution_plan_error is None

              checks[
                  "execution_plan_mode_is_plan"
              ] = (
                  execution_plan.get(
                      "mode"
                  )
                  == "plan"
              )

              checks[
                  "execution_plan_input_contract_present"
              ] = input_contract_error is None

              checks[
                  "execution_plan_input_contract_mode_matches"
              ] = (
                  input_contract.get(
                      "mode"
                  )
                  == mode
              )

              checks[
                  "execution_plan_input_contract_has_no_violations"
              ] = (
                  input_contract_error is None
                  and not input_contract.get(
                      "policy_violations",
                      [],
                  )
              )

              checks[
                  "execute_stage_may_overwrite_execution_plan"
              ] = input_contract.get(
                  "execute_stage_may_overwrite_execution_plan"
              )

              if execution_plan_error:
                  violations.append(
                      execution_plan_error
                  )

              elif execution_plan.get(
                  "mode"
              ) != "plan":
                  violations.append(
                      "execution_plan.json must remain a plan-stage artifact with mode=plan."
                  )

              if input_contract_error:
                  violations.append(
                      input_contract_error
                  )

              else:
                  if input_contract.get(
                      "mode"
                  ) != mode:
                      violations.append(
                          "execution_plan_input_contract mode does not match the requested executor mode."
                      )

                  input_contract_violations = (
                      input_contract.get(
                          "policy_violations",
                          [],
                      )
                  )

                  if input_contract_violations:
                      violations.extend(
                          str(item)
                          for item
                          in input_contract_violations
                      )

                  if input_contract.get(
                      "execute_stage_may_overwrite_execution_plan"
                  ) is not False:
                      violations.append(
                          "execution_plan_input_contract must forbid execute-stage plan overwrite."
                      )

              #
              # Dry run requires the plan contract but not human
              # execution approval.
              #

              if mode == "dry_run_execute":
                  status = (
                      "allowed"
                      if not violations
                      else "blocked"
                  )

                  reason = (
                      "Dry-run execution prerequisites satisfied."
                      if status == "allowed"
                      else "Dry-run execution prerequisites failed."
                  )

              #
              # --------------------------------------------------
              # Execute-only checks
              # --------------------------------------------------
              #

              else:
                  approval, approval_error = load_json(
                      approval_path
                  )

                  approval_matching, matching_error = (
                      load_json(
                          approval_matching_path
                      )
                  )

                  fingerprint, fingerprint_error = (
                      load_json(
                          fingerprint_path
                      )
                  )

                  plan_audit, plan_audit_error = (
                      load_json(
                          plan_audit_path
                      )
                  )

                  mutation_plan, mutation_error = (
                      load_json(
                          mutation_plan_path
                      )
                  )

                  persistence_contract, persistence_error = (
                      load_json(
                          persistence_contract_path
                      )
                  )

                  pre_audit, pre_audit_error = (
                      load_json(
                          pre_audit_path
                      )
                  )

                  execution_policy, policy_error = (
                      load_json(
                          execution_policy_path
                      )
                  )

                  approved_bundle = approval.get(
                      "approved_execution_bundle",
                      {},
                  )

                  approval_governance = approval.get(
                      "governance",
                      {},
                  )

                  phase_boundary_contract = approval.get(
                      "phase_boundary_contract",
                      {},
                  )

                  checks.update(
                      {
                          "pre_audit_report_present":
                              pre_audit_error is None,

                          "approval_artifact_present":
                              approval_error is None,

                          "approval_schema_supported":
                              approval.get(
                                  "schema"
                              )
                              == "rga.human_execution_approval.v2.1",

                          "approved_true":
                              approval.get(
                                  "approved"
                              )
                              is True,

                          "approval_phrase_valid":
                              approval.get(
                                  "approval_phrase"
                              )
                              == required_approval_phrase,

                          "approval_scope_valid":
                              approval.get(
                                  "approval_scope"
                              )
                              == "maintenance_executor",

                          "human_review_completed":
                              approval_governance.get(
                                  "human_review_completed"
                              )
                              is True,

                          "plan_audit_completed":
                              approval_governance.get(
                                  "plan_audit_completed"
                              )
                              is True,

                          "bot_2_may_execute":
                              approval_governance.get(
                                  "bot_2_may_execute"
                              )
                              is True,

                          "bot_2_may_self_approve":
                              approval_governance.get(
                                  "bot_2_may_self_approve"
                              ),

                          "post_audit_required":
                              approval_governance.get(
                                  "post_audit_required"
                              )
                              is True,

                          "completed_phases_immutable":
                              phase_boundary_contract.get(
                                  "completed_phases_immutable"
                              )
                              is True,

                          "fingerprint_artifact_present":
                              fingerprint_error is None,

                          "database_mutation_plan_present":
                              mutation_error is None,

                          "persistence_contract_present":
                              persistence_error is None,

                          "approval_matching_artifact_present":
                              matching_error is None,

                          "approval_matches_execution_plan":
                              approval_matching.get(
                                  "approval_matches_execution_plan"
                              )
                              is True,

                          "approval_matches_database_mutation_plan":
                              approval_matching.get(
                                  "approval_matches_database_mutation_plan"
                              )
                              is True,

                          "all_required_matches_passed":
                              approval_matching.get(
                                  "all_required_matches_passed"
                              )
                              is True,

                          "plan_audit_evidence_present":
                              plan_audit_error is None,

                          "plan_audit_ready_for_human_review":
                              plan_audit.get(
                                  "verdict"
                              )
                              == "ready_for_human_review",

                          "execution_policy_audit_present":
                              policy_error is None,

                          "execution_policy_execution_authority":
                              execution_policy.get(
                                  "execution_authority"
                              )
                              is True,

                          "execution_policy_approval_authority":
                              execution_policy.get(
                                  "approval_authority"
                              ),

                          "execution_policy_self_approval":
                              execution_policy.get(
                                  "self_approval"
                              ),
                      }
                  )

                  required_files = [
                      (
                          pre_audit_error,
                          "Runtime Auditor pre-audit report",
                      ),
                      (
                          approval_error,
                          "human execution approval",
                      ),
                      (
                          fingerprint_error,
                          "execution plan fingerprint",
                      ),
                      (
                          mutation_error,
                          "database mutation plan",
                      ),
                      (
                          persistence_error,
                          "persistence contract",
                      ),
                      (
                          matching_error,
                          "approval artifact matching",
                      ),
                      (
                          plan_audit_error,
                          "plan audit evidence",
                      ),
                      (
                          policy_error,
                          "execution policy audit",
                      ),
                  ]

                  for error, label in required_files:
                      if error:
                          violations.append(
                              f"Required {label} is unavailable: {error}"
                          )

                  if approval_error is None:
                      if approval.get(
                          "schema"
                      ) != "rga.human_execution_approval.v2.1":
                          violations.append(
                              "Unsupported human execution approval schema."
                          )

                      if approval.get(
                          "approved"
                      ) is not True:
                          violations.append(
                              "Human execution approval does not set approved=true."
                          )

                      if approval.get(
                          "approval_phrase"
                      ) != required_approval_phrase:
                          violations.append(
                              "Human execution approval phrase is invalid."
                          )

                      if approval.get(
                          "approval_scope"
                      ) != "maintenance_executor":
                          violations.append(
                              "Human execution approval scope is invalid."
                          )

                      if not isinstance(
                          approved_bundle,
                          dict,
                      ):
                          violations.append(
                              "approved_execution_bundle must be an object."
                          )

                      if approval_governance.get(
                          "human_review_completed"
                      ) is not True:
                          violations.append(
                              "Human review is not marked complete."
                          )

                      if approval_governance.get(
                          "plan_audit_completed"
                      ) is not True:
                          violations.append(
                              "Bot #1 plan-audit is not marked complete."
                          )

                      if approval_governance.get(
                          "bot_2_may_execute"
                      ) is not True:
                          violations.append(
                              "Human approval does not authorize Bot #2 execution."
                          )

                      if approval_governance.get(
                          "bot_2_may_self_approve"
                      ) is not False:
                          violations.append(
                              "Bot #2 self-approval must be false."
                          )

                      if approval_governance.get(
                          "post_audit_required"
                      ) is not True:
                          violations.append(
                              "Bot #1 post-audit must remain required."
                          )

                      if phase_boundary_contract.get(
                          "completed_phases_immutable"
                      ) is not True:
                          violations.append(
                              "Human approval must preserve Completed Phases as immutable."
                          )

                  if matching_error is None:
                      if approval_matching.get(
                          "all_required_matches_passed"
                      ) is not True:
                          violations.append(
                              "Approval artifact matching did not pass all required matches."
                          )

                      matching_violations = (
                          approval_matching.get(
                              "policy_violations",
                              [],
                          )
                      )

                      if matching_violations:
                          violations.extend(
                              str(item)
                              for item
                              in matching_violations
                          )

                  if plan_audit_error is None:
                      if plan_audit.get(
                          "verdict"
                      ) != "ready_for_human_review":
                          violations.append(
                              "Plan audit evidence is not ready_for_human_review."
                          )

                  if policy_error is None:
                      if execution_policy.get(
                          "execution_authority"
                      ) is not True:
                          violations.append(
                              "Execution policy audit does not grant execution authority for execute mode."
                          )

                      if execution_policy.get(
                          "approval_authority"
                      ) is not False:
                          violations.append(
                              "Execution policy audit must deny Bot #2 approval authority."
                          )

                      if execution_policy.get(
                          "self_approval"
                      ) is not False:
                          violations.append(
                              "Execution policy audit must deny self-approval."
                          )

                  status = (
                      "allowed"
                      if not violations
                      else "blocked"
                  )

                  reason = (
                      "All execute-stage prerequisites satisfied."
                      if status == "allowed"
                      else "One or more execute-stage prerequisites failed."
                  )

          #
          # Keep diagnostic messages deterministic.
          #

          violations = list(
              dict.fromkeys(
                  violations
              )
          )

          warnings = list(
              dict.fromkeys(
                  warnings
              )
          )

          payload = {
              "schema":
                  "rga.execution_gate.v1.1",

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "requested_mode":
                  mode,

              "status":
                  status,

              "allowed":
                  status == "allowed",

              "reason":
                  reason,

              "approval_authority":
                  "human",

              "execution_authority":
                  (
                      "bot_2"
                      if (
                          mode == "execute"
                          and status == "allowed"
                      )
                      else None
                  ),

              "checks":
                  checks,

              "policy_violations":
                  violations,

              "warnings":
                  warnings,

              "source_artifacts": {
                  "pre_audit_report":
                      str(
                          pre_audit_path
                      ),

                  "execution_plan":
                      str(
                          execution_plan_path
                      ),

                  "execution_plan_input_contract":
                      str(
                          input_contract_path
                      ),

                  "execution_plan_fingerprint":
                      str(
                          fingerprint_path
                      ),

                  "database_mutation_plan":
                      str(
                          mutation_plan_path
                      ),

                  "persistence_contract":
                      str(
                          persistence_contract_path
                      ),

                  "plan_audit_evidence":
                      str(
                          plan_audit_path
                      ),

                  "human_execution_approval":
                      str(
                          approval_path
                      ),

                  "approval_artifact_matching":
                      str(
                          approval_matching_path
                      ),

                  "execution_policy_audit":
                      str(
                          execution_policy_path
                      ),
              },
          }

          output_path.write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )

          github_output = os.environ.get(
              "GITHUB_OUTPUT"
          )

          if github_output:
              with open(
                  github_output,
                  "a",
                  encoding="utf-8",
              ) as output:
                  output.write(
                      f"status={status}\n"
                  )
                  output.write(
                      "allowed="
                      f"{str(status == 'allowed').lower()}\n"
                  )
                  output.write(
                      f"reason={reason}\n"
                  )

          print(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              )
          )
          PY

          python3 artifacts/evaluate_execution_gate.py

          rm -f artifacts/evaluate_execution_gate.py           

      ############################################################
      # Apply Execution Result
      ############################################################

      - name: Apply Execution Result
        if: always()
        shell: bash
        run: |
          mkdir -p artifacts

          python - <<'PY'
          import json
          from pathlib import Path

          apply_path = Path(
              "artifacts/apply_execution_result.json"
          )

          report_path = Path(
              "artifacts/runtime_executor_report.json"
          )

          result = {}

          #
          # Prefer canonical artifact emitted directly by runtime_executor.py.
          #

          if apply_path.exists() and apply_path.stat().st_size > 0:
              try:
                  result = json.loads(
                      apply_path.read_text(
                          encoding="utf-8",
                      )
                  )
              except Exception as exc:
                  result = {
                      "schema":
                          "rga.apply_execution_result.v1.0",

                      "execution_attempted":
                          False,

                      "execution_performed":
                          False,

                      "written_files":
                          [],

                      "policy_violations": [
                          f"Could not parse existing apply_execution_result.json: {exc}"
                      ],

                      "post_audit_required":
                          True,
                  }

          #
          # Fallback: extract from runtime_executor_report.json.
          #

          if not result and report_path.exists():
              try:
                  executor_report = json.loads(
                      report_path.read_text(
                          encoding="utf-8",
                      )
                  )

                  result = executor_report.get(
                      "apply_execution_result",
                      {},
                  )

              except Exception as exc:
                  result = {
                      "schema":
                          "rga.apply_execution_result.v1.0",

                      "execution_attempted":
                          False,

                      "execution_performed":
                          False,

                      "written_files":
                          [],

                      "policy_violations": [
                          f"Could not parse runtime_executor_report.json: {exc}"
                      ],

                      "post_audit_required":
                          True,
                  }

          #
          # Final fallback.
          #

          if not result:
              result = {
                  "schema":
                      "rga.apply_execution_result.v1.0",

                  "execution_attempted":
                      False,

                  "execution_performed":
                      False,

                  "written_files":
                      [],

                  "policy_violations":
                      [],

                  "post_audit_required":
                      True,

                  "note":
                      "No apply_execution_result was emitted by runtime_executor.py."
              }

          apply_path.write_text(
              json.dumps(
                  result,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY
          
      ############################################################
      # Executor Write Manifest
      ############################################################

      - name: Executor Write Manifest
        if: always()
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p artifacts

          cat > artifacts/build_executor_write_manifest.py <<'PY'
          import json
          from datetime import datetime, timezone
          from pathlib import Path

          manifest_path = Path(
              "artifacts/executor_write_manifest.json"
          )

          apply_path = Path(
              "artifacts/apply_execution_result.json"
          )

          #
          # Prefer runtime_executor.py emitted artifact.
          #

          if manifest_path.exists() and manifest_path.stat().st_size > 0:
              raise SystemExit(0)

          apply_result = {}

          if apply_path.exists() and apply_path.stat().st_size > 0:
              try:
                  apply_result = json.loads(
                      apply_path.read_text(
                          encoding="utf-8",
                      )
                  )
              except Exception:
                  apply_result = {}

          policy_violations = list(
              apply_result.get(
                  "policy_violations",
                  [],
              )
          )

          executor_written_files = list(
              apply_result.get(
                  "executor_written_files",
                  apply_result.get(
                      "written_files",
                      [],
                  ),
              )
          )

          payload = {
              "schema":
                  "rga.executor_write_manifest.v1.0",

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "producer":
                  "workflow_fallback",

              "executor":
                  "runtime_executor.py",

              "executor_mode":
                  apply_result.get(
                      "executor_mode",
                  ),

              "execution_attempted":
                  apply_result.get(
                      "execution_attempted",
                      False,
                  ),

              "execution_performed":
                  apply_result.get(
                      "execution_performed",
                      False,
                  ),

              "patch_probe_enabled":
                  apply_result.get(
                      "patch_probe_enabled",
                      False,
                  ),

              "patch_probe_performed":
                  apply_result.get(
                      "patch_probe_performed",
                      False,
                  ),

              "patch_certifiable":
                  apply_result.get(
                      "patch_certifiable",
                      False,
                  ),

              "execution_verdict":
                  apply_result.get(
                      "execution_verdict",
                      "fallback_manifest",
                  ),

              "executor_written_files":
                  executor_written_files,

              "allowed_scope_only":
                  len(policy_violations) == 0,

              "protected_scope_touched":
                  apply_result.get(
                      "protected_scope_touched",
                      False,
                  ),

              "completed_phases_modified":
                  apply_result.get(
                      "completed_phase_mutation_performed",
                      False,
                  ),

              "database_mutation_performed":
                  apply_result.get(
                      "db_mutation_performed",
                      False,
                  ),

              "source_asset_deletion_performed":
                  apply_result.get(
                      "source_asset_deletion_performed",
                      False,
                  ),

              "policy_violations":
                  policy_violations,

              "post_audit_required":
                  apply_result.get(
                      "post_audit_required",
                      True,
                  ),

              "note":
                  (
                      "Fallback executor_write_manifest generated by workflow "
                      "because runtime_executor.py did not emit it."
                  ),
          }

          manifest_path.write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY

          python artifacts/build_executor_write_manifest.py
          

          
      ############################################################
      # Apply Repository Changes
      ############################################################

      - name: Apply Repository Changes
        if: always()
        env:
          EXECUTOR_MODE: ${{ inputs.executor_mode }}
        run: |
          set +e

          python - <<'PY'
          import json
          import os
          from pathlib import Path
          import subprocess

          mode = os.environ.get("EXECUTOR_MODE", "")

          apply_result_file = Path(
              "artifacts/apply_execution_result.json"
          )

          repository_changes_applied = False
          changed_files = []
          written_files = []
          notes = []
          policy_violations = []

          if apply_result_file.exists():

              try:
                  result = json.loads(
                      apply_result_file.read_text(
                          encoding="utf-8"
                      )
                  )

                  written_files = result.get(
                      "written_files",
                      [],
                  )

                  policy_violations = result.get(
                      "policy_violations",
                      [],
                  )

              except Exception as exc:
                  notes.append(
                      f"Could not parse apply_execution_result.json: {exc}"
                  )

          else:
              notes.append(
                  "apply_execution_result.json not found."
              )

          protected_tokens = [
              "canonical_row",
              "pattern_logic",
              "tips_generation",
              "personalization",
              "localization",
              "recommendation_logic",
              "Phase 1",
              "Phase 2",
              "Phase 3",
              "Phase 4 - Personalization",
              "Phase 4.5 - Localization",
              "Phase 5 - Productionization",
              "Phase 6 - Hardening and Scaling",
              "Phase 7 - Games Recommendation",
          ]

          allowed_roots = [
              "tools/",
              "artifacts/",
              ".github/workflows/",
              "docs/",
          ]

          for item in written_files:
              normalized = str(item).replace("\\", "/")

              if any(
                  token.lower() in normalized.lower()
                  for token in protected_tokens
              ):
                  policy_violations.append(
                      f"Protected scope change rejected: {normalized}"
                  )
                  continue

              if not any(
                  normalized.startswith(prefix)
                  for prefix in allowed_roots
              ):
                  policy_violations.append(
                      f"Change outside allowed roots rejected: {normalized}"
                  )
                  continue

              changed_files.append(
                  normalized
              )

          #
          # Capture actual git working tree changes.
          #

          try:
              proc = subprocess.run(
                  [
                      "git",
                      "status",
                      "--short",
                  ],
                  check=False,
                  capture_output=True,
                  text=True,
              )

              git_status_short = proc.stdout.splitlines()

          except Exception as exc:
              git_status_short = []
              notes.append(
                  f"Could not inspect git status: {exc}"
              )

          repository_changes_applied = bool(
              changed_files
              or git_status_short
          )

          payload = {
              "schema":
                  "rga.repository_changes.v1.1",

              "executor_mode":
                  mode,

              "repository_changes_applied":
                  repository_changes_applied,

              "changed_files_from_executor":
                  changed_files,

              "git_status_short":
                  git_status_short,

              "policy_violations":
                  policy_violations,

              "notes":
                  notes,

              "allowed_scope":
                  allowed_roots,

              "protected_scopes":
                  protected_tokens,

              "completed_phases_immutable":
                  True,

              "governance_note": (
                  "Repository change evidence is derived from runtime_executor "
                  "outputs and git status. This step does not approve changes."
              )
          }

          Path(
              "artifacts/repository_changes.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY


      ############################################################
      # Execution Commit Manifest
      ############################################################

      - name: Execution Commit Manifest
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          repository_changes = Path(
              "artifacts/repository_changes.json"
          )

          changed_files = []
          git_status_short = []
          policy_violations = []

          if repository_changes.exists():

              data = json.loads(
                  repository_changes.read_text(
                      encoding="utf-8"
                  )
              )

              changed_files = data.get(
                  "changed_files_from_executor",
                  []
              )

              git_status_short = data.get(
                  "git_status_short",
                  []
              )

              policy_violations = data.get(
                  "policy_violations",
                  []
              )

          commit_required = bool(
              git_status_short
          ) and not policy_violations

          payload = {

              "schema":
                  "rga.execution_commit_manifest.v1.1",

              "commit_required":
                  commit_required,

              "commit_candidate_files":
                  changed_files,

              "git_status_short":
                  git_status_short,

              "policy_violations":
                  policy_violations,

              "commit_message": (
                  "[RGA Executor] Apply approved maintenance execution"
              ),

              "branch_name":
                  "maintenance/executor-generated",

              "human_review_required":
                  True,

              "post_audit_required":
                  True,

              "governance_note": (
                  "Commit manifest is a candidate only. "
                  "Bot #2 does not self-approve. Human review remains required."
              )
          }

          Path(
              "artifacts/execution_commit_manifest.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY


      ############################################################
      # Execution Pull Request Candidate
      ############################################################

      - name: Execution Pull Request Candidate
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          commit_manifest = Path(
              "artifacts/execution_commit_manifest.json"
          )

          pull_request_required = False
          branch_name = "maintenance/executor-generated"
          title = "[RGA Executor] Approved maintenance updates"
          body = (
              "Generated by RGA Executor. "
              "Bot #1 post-audit remains required."
          )
          commit_candidate_files = []
          policy_violations = []

          if commit_manifest.exists():

              data = json.loads(
                  commit_manifest.read_text(
                      encoding="utf-8"
                  )
              )

              pull_request_required = data.get(
                  "commit_required",
                  False
              )

              branch_name = data.get(
                  "branch_name",
                  branch_name,
              )

              commit_candidate_files = data.get(
                  "commit_candidate_files",
                  [],
              )

              policy_violations = data.get(
                  "policy_violations",
                  [],
              )

          payload = {

              "schema":
                  "rga.execution_pull_request_candidate.v1.1",

              "pull_request_required":
                  pull_request_required,

              "branch_name":
                  branch_name,

              "title":
                  title,

              "body":
                  body,

              "commit_candidate_files":
                  commit_candidate_files,

              "policy_violations":
                  policy_violations,

              "required_reviewers": [
                  "human_governor"
              ],

              "required_gates": [

                  "execution_gate",

                  "approval_artifact_matching",

                  "execution_scope_audit",

                  "execution_policy_audit",

                  "post_audit"
              ],

              "governance_note": (
                  "This file records a PR candidate. "
                  "Actual PR creation occurs only in the gated PR creation step."
              )
          }

          Path(
              "artifacts/execution_pull_request_candidate.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY
          
      ############################################################
      # Create Execution Branch and Commit
      ############################################################

      - name: Create Execution Branch and Commit
        if: ${{ always() && inputs.executor_mode == 'execute' }}
        run: |
          set +e

          python - <<'PY'
          import json
          from pathlib import Path

          manifest_file = Path(
              "artifacts/execution_commit_manifest.json"
          )

          result = {
              "schema":
                  "rga.execution_git_commit_result.v1.0",

              "commit_attempted":
                  False,

              "commit_created":
                  False,

              "branch_name":
                  "maintenance/executor-generated",

              "commit_message":
                  "[RGA Executor] Apply approved maintenance execution",

              "reason":
                  None,
          }

          if manifest_file.exists():
              data = json.loads(
                  manifest_file.read_text(
                      encoding="utf-8"
                  )
              )

              result["commit_attempted"] = bool(
                  data.get(
                      "commit_required",
                      False
                  )
              )

              result["branch_name"] = data.get(
                  "branch_name",
                  result["branch_name"]
              )

              result["commit_message"] = data.get(
                  "commit_message",
                  result["commit_message"]
              )

              if not data.get("commit_required", False):
                  result["reason"] = "Commit not required."

              if data.get("policy_violations"):
                  result["reason"] = "Commit blocked by policy violations."

          else:
              result["reason"] = "execution_commit_manifest.json not found."

          Path(
              "artifacts/execution_git_commit_result.json"
          ).write_text(
              json.dumps(
                  result,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY

          commit_required=$(python - <<'PY'
          import json
          from pathlib import Path

          p = Path("artifacts/execution_commit_manifest.json")

          if not p.exists():
              print("false")
          else:
              data = json.loads(p.read_text(encoding="utf-8"))
              if data.get("commit_required") and not data.get("policy_violations"):
                  print("true")
              else:
                  print("false")
          PY
          )

          if [ "$commit_required" != "true" ]; then
            echo "No commit required or commit blocked by policy."
            exit 0
          fi

          branch_name=$(python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path("artifacts/execution_commit_manifest.json").read_text(encoding="utf-8"))
          print(data.get("branch_name", "maintenance/executor-generated"))
          PY
          )

          commit_message=$(python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path("artifacts/execution_commit_manifest.json").read_text(encoding="utf-8"))
          print(data.get("commit_message", "[RGA Executor] Apply approved maintenance execution"))
          PY
          )

          git config user.name "rga-executor-bot"
          git config user.email "rga-executor-bot@users.noreply.github.com"

          git checkout -B "$branch_name"

          git add tools artifacts .github/workflows docs 2>/dev/null || true

          if git diff --cached --quiet; then
            echo "No staged changes."
            exit 0
          fi

          git commit -m "$commit_message"

          git push --set-upstream origin "$branch_name"

          python - <<'PY'
          import json
          from pathlib import Path

          p = Path("artifacts/execution_git_commit_result.json")
          data = json.loads(p.read_text(encoding="utf-8"))

          data["commit_created"] = True
          data["reason"] = "Commit created and branch pushed."

          p.write_text(
              json.dumps(data, indent=2, ensure_ascii=False),
              encoding="utf-8",
          )
          PY

      ############################################################
      # Create Pull Request Candidate
      #
      # Bot #2 execute mode:
      #
      #   - Creates a PR only when execution_pull_request_candidate.json
      #     says pull_request_required=true.
      #
      #   - Records the result in:
      #
      #       artifacts/execution_pull_request_result.json
      #
      #   - Does not approve or merge the PR.
      #
      #   - Human review remains required.
      ############################################################

      - name: Create Pull Request Candidate
        if: ${{ always() && inputs.executor_mode == 'execute' }}
        shell: bash
        env:
          GH_TOKEN: ${{ steps.artifact_token.outputs.token }}
          BASE_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          set +e

          mkdir -p artifacts

          cat > artifacts/create_pull_request_candidate.py <<'PY'
          import json
          import os
          import subprocess
          from datetime import datetime, timezone
          from pathlib import Path

          candidate_file = Path(
              "artifacts/execution_pull_request_candidate.json"
          )

          url_file = Path(
              "artifacts/execution_pull_request_url.txt"
          )

          stderr_file = Path(
              "artifacts/execution_pull_request_stderr.txt"
          )

          result_file = Path(
              "artifacts/execution_pull_request_result.json"
          )

          payload = {
              "schema":
                  "rga.execution_pull_request_result.v1.1",

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "pull_request_attempted":
                  False,

              "pull_request_created":
                  False,

               "reason": 
                  "Draft PR candidate prepared."   

              "pull_request_url":
                  None,

              "branch_name":
                  None,

              "base_branch":
                  os.environ.get(
                      "BASE_BRANCH",
                      "main",
                  ),

              "return_code":
                  None,

              "stderr":
                  None,

              "reason":
                  None,

              "post_audit_required":
                  True,

              "human_review_required":
                  True,

              "bot_2_may_approve":
                  False,

              "bot_2_may_merge":
                  False,
          }

          try:
              if not candidate_file.exists():
                  payload[
                      "reason"
                  ] = (
                      "execution_pull_request_candidate.json not found."
                  )

                  result_file.write_text(
                      json.dumps(
                          payload,
                          indent=2,
                          ensure_ascii=False,
                      ),
                      encoding="utf-8",
                  )

                  raise SystemExit(0)

              candidate = json.loads(
                  candidate_file.read_text(
                      encoding="utf-8",
                      errors="ignore",
                  )
              )

              if not isinstance(
                  candidate,
                  dict,
              ):
                  payload[
                      "reason"
                  ] = (
                      "execution_pull_request_candidate.json is not a JSON object."
                  )

                  result_file.write_text(
                      json.dumps(
                          payload,
                          indent=2,
                          ensure_ascii=False,
                      ),
                      encoding="utf-8",
                  )

                  raise SystemExit(0)

              pull_request_required = bool(
                  candidate.get(
                      "pull_request_required",
                      False,
                  )
              )

              branch_name = candidate.get(
                  "branch_name",
                  "maintenance/executor-generated",
              )

              pr_title = candidate.get(
                  "title",
                  "[RGA Executor] Approved maintenance updates",
              )

              pr_body = candidate.get(
                  "body",
                  (
                      "Generated by RGA Executor. "
                      "Bot #1 post-audit remains required."
                  ),
              )

              payload[
                  "branch_name"
              ] = branch_name

              if not pull_request_required:
                  payload[
                      "reason"
                  ] = (
                      "Pull request not required."
                  )

                  result_file.write_text(
                      json.dumps(
                          payload,
                          indent=2,
                          ensure_ascii=False,
                      ),
                      encoding="utf-8",
                  )

                  raise SystemExit(0)

              payload[
                  "pull_request_attempted"
              ] = True

              cmd = [
                  "gh",
                  "pr",
                  "create",
                  "--head",
                  branch_name,
                  "--base",
                  os.environ.get(
                      "BASE_BRANCH",
                      "main",
                  ),
                  "--title",
                  pr_title,
                  "--body",
                  pr_body,
              ]

              proc = subprocess.run(
                  cmd,
                  capture_output=True,
                  text=True,
                  check=False,
              )

              stdout_text = (
                  proc.stdout
                  or ""
              ).strip()

              stderr_text = (
                  proc.stderr
                  or ""
              ).strip()

              url_file.write_text(
                  stdout_text,
                  encoding="utf-8",
              )

              stderr_file.write_text(
                  stderr_text,
                  encoding="utf-8",
              )

              payload[
                  "return_code"
              ] = proc.returncode

              payload[
                  "stderr"
              ] = stderr_text

              if (
                  proc.returncode == 0
                  and stdout_text
              ):
                  payload[
                      "pull_request_created"
                  ] = True

                  payload[
                      "pull_request_url"
                  ] = stdout_text

                  payload[
                      "reason"
                  ] = (
                      "Pull request created."
                  )

              else:
                  payload[
                      "pull_request_created"
                  ] = False

                  payload[
                      "pull_request_url"
                  ] = (
                      stdout_text
                      if stdout_text
                      else None
                  )

                  payload[
                      "reason"
                  ] = (
                      "gh pr create did not create a pull request."
                  )

          except Exception as exc:
              payload[
                  "reason"
              ] = (
                  f"Create pull request candidate failed: {exc}"
              )

          finally:
              result_file.write_text(
                  json.dumps(
                      payload,
                      indent=2,
                      ensure_ascii=False,
                  ),
                  encoding="utf-8",
              )
          PY

          python artifacts/create_pull_request_candidate.py

          echo "Create Pull Request Candidate completed."
          echo "Result artifact: artifacts/execution_pull_request_result.json"

          exit 0
          
      ############################################################
      # Execution Provenance
      ############################################################

      - name: Execution Provenance
        if: always()
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p artifacts

          cat > artifacts/build_execution_provenance.py <<'PY'
          import json
          from datetime import datetime, timezone
          from pathlib import Path

          provenance_path = Path(
              "artifacts/execution_provenance.json"
          )

          #
          # Prefer runtime_executor.py emitted artifact.
          #

          if provenance_path.exists() and provenance_path.stat().st_size > 0:
              raise SystemExit(0)

          def read_json(path: Path):
              if not path.exists() or path.stat().st_size == 0:
                  return {}

              try:
                  return json.loads(
                      path.read_text(
                          encoding="utf-8",
                      )
                  )
              except Exception:
                  return {}

          apply_result = read_json(
              Path("artifacts/apply_execution_result.json")
          )

          executor_manifest = read_json(
              Path("artifacts/executor_write_manifest.json")
          )

          repository_changes = read_json(
              Path("artifacts/repository_changes.json")
          )

          execution_commit_manifest = read_json(
              Path("artifacts/execution_commit_manifest.json")
          )

          execution_git_commit_result = read_json(
              Path("artifacts/execution_git_commit_result.json")
          )

          execution_pull_request_candidate = read_json(
              Path("artifacts/execution_pull_request_candidate.json")
          )

          execution_pull_request_result = read_json(
              Path("artifacts/execution_pull_request_result.json")
          )

          executor_written_files = list(
              executor_manifest.get(
                  "executor_written_files",
                  [],
              )
          )

          changed_files_from_executor = list(
              repository_changes.get(
                  "changed_files_from_executor",
                  [],
              )
          )

          commit_candidate_files = list(
              execution_commit_manifest.get(
                  "commit_candidate_files",
                  [],
              )
          )

          policy_violations = []

          policy_violations.extend(
              executor_manifest.get(
                  "policy_violations",
                  [],
              )
          )

          policy_violations.extend(
              repository_changes.get(
                  "policy_violations",
                  [],
              )
          )

          policy_violations.extend(
              execution_commit_manifest.get(
                  "policy_violations",
                  [],
              )
          )

          policy_violations.extend(
              execution_pull_request_candidate.get(
                  "policy_violations",
                  [],
              )
          )

          protected_scope_touched = bool(
              executor_manifest.get(
                  "protected_scope_touched",
                  False,
              )
          )

          completed_phases_modified = bool(
              executor_manifest.get(
                  "completed_phases_modified",
                  False,
              )
          )

          database_mutation_performed = bool(
              executor_manifest.get(
                  "database_mutation_performed",
                  False,
              )
          )

          source_asset_deletion_performed = bool(
              executor_manifest.get(
                  "source_asset_deletion_performed",
                  False,
              )
          )

          if policy_violations:
              verdict = "patch_provenance_blocked"

          elif (
              protected_scope_touched
              or completed_phases_modified
              or database_mutation_performed
              or source_asset_deletion_performed
          ):
              verdict = "patch_provenance_blocked"

          elif (
              executor_written_files
              and set(executor_written_files) == set(commit_candidate_files)
          ):
              verdict = "patch_provenance_ready"

          elif executor_written_files:
              verdict = "patch_provenance_incomplete"

          else:
              verdict = "traceable_no_patch"

          payload = {
              "schema":
                  "rga.execution_provenance.v1.0",

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "producer":
                  "workflow_fallback",

              "lifecycle_stage":
                  "execute",

              "source_artifacts": {
                  "apply_execution_result":
                      "artifacts/apply_execution_result.json",
                  "executor_write_manifest":
                      "artifacts/executor_write_manifest.json",
                  "repository_changes":
                      "artifacts/repository_changes.json",
                  "execution_commit_manifest":
                      "artifacts/execution_commit_manifest.json",
                  "execution_git_commit_result":
                      "artifacts/execution_git_commit_result.json",
                  "execution_pull_request_candidate":
                      "artifacts/execution_pull_request_candidate.json",
                  "execution_pull_request_result":
                      "artifacts/execution_pull_request_result.json",
              },

              "executor_provenance": {
                  "execution_attempted":
                      apply_result.get(
                          "execution_attempted",
                          False,
                      ),
                  "execution_performed":
                      apply_result.get(
                          "execution_performed",
                          False,
                      ),
                  "executor_written_files":
                      executor_written_files,
                  "changed_files_from_executor":
                      changed_files_from_executor,
                  "patch_probe_performed":
                      executor_manifest.get(
                          "patch_probe_performed",
                          False,
                      ),
                  "patch_certifiable":
                      executor_manifest.get(
                          "patch_certifiable",
                          False,
                      ),
                  "execution_verdict":
                      executor_manifest.get(
                          "execution_verdict",
                      ),
              },

              "repository_state": {
                  "git_status_short":
                      repository_changes.get(
                          "git_status_short",
                          [],
                      ),
              },

              "commit_provenance": {
                  "commit_required":
                      execution_commit_manifest.get(
                          "commit_required",
                          False,
                      ),
                  "commit_attempted":
                      execution_git_commit_result.get(
                          "commit_attempted",
                          False,
                      ),
                  "commit_created":
                      execution_git_commit_result.get(
                          "commit_created",
                          False,
                      ),
                  "branch_name":
                      execution_commit_manifest.get(
                          "branch_name",
                          execution_git_commit_result.get(
                              "branch_name",
                          ),
                      ),
                  "commit_message":
                      execution_commit_manifest.get(
                          "commit_message",
                          execution_git_commit_result.get(
                              "commit_message",
                          ),
                      ),
                  "commit_candidate_files":
                      commit_candidate_files,
              },

              "pr_provenance": {
                  "pull_request_required":
                      execution_pull_request_candidate.get(
                          "pull_request_required",
                          False,
                      ),
                  "pull_request_attempted":
                      execution_pull_request_result.get(
                          "pull_request_attempted",
                          False,
                      ),
                  "pull_request_created":
                      execution_pull_request_result.get(
                          "pull_request_created",
                          False,
                      ),
                  "pull_request_url":
                      execution_pull_request_result.get(
                          "pull_request_url",
                      ),
                  "branch_name":
                      execution_pull_request_candidate.get(
                          "branch_name",
                      ),
                  "title":
                      execution_pull_request_candidate.get(
                          "title",
                      ),
                  "required_gates":
                      execution_pull_request_candidate.get(
                          "required_gates",
                          [],
                      ),
              },

              "policy": {
                  "allowed_scope_only":
                      executor_manifest.get(
                          "allowed_scope_only",
                          False,
                      ),
                  "protected_scope_touched":
                      protected_scope_touched,
                  "completed_phases_modified":
                      completed_phases_modified,
                  "database_mutation_performed":
                      database_mutation_performed,
                  "source_asset_deletion_performed":
                      source_asset_deletion_performed,
                  "policy_violations":
                      policy_violations,
              },

              "provenance_verdict":
                  verdict,

              "note":
                  (
                      "Fallback execution_provenance generated by workflow "
                      "because runtime_executor.py did not emit it."
                  ),
          }

          provenance_path.write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY

          python artifacts/build_execution_provenance.py
          
      ############################################################
      # Execution Manifest
      ############################################################

      - name: Execution Manifest
        if: always()
        shell: bash
        run: |
          mkdir -p artifacts
          find artifacts -type f | sort > artifacts/execution_manifest.txt

      ############################################################
      # Execution Lineage
      #
      # Captures the lifecycle relationship between:
      #
      #   Plan Contracts
      #   Audit Contracts
      #   Approval Contracts
      #   Execution Evidence
      #
      # This artifact is descriptive provenance only.
      ############################################################

      - name: Execution Lineage
        if: always()
        shell: bash
        run: |
          mkdir -p artifacts

          python - <<'PY'
          import json
          from datetime import datetime, timezone
          from pathlib import Path

          payload = {
              "schema":
                  "rga.execution_lineage.v1.2",

              "generated_at":
                  datetime.now(
                      timezone.utc
                  ).replace(
                      microsecond=0
                  ).isoformat(),

              "artifact_roles": {
                  "proposal_contracts": [
                      "execution_plan",
                      "database_mutation_plan",
                  ],

                  "governance_contracts": [
                      "persistence_contract",
                  ],

                  "integrity_contracts": [
                      "execution_plan_fingerprint",
                  ],

                  "audit_contracts": [
                      "plan_audit_evidence",
                  ],

                  "approval_contracts": [
                      "human_execution_approval",
                      "approval_artifact_matching",
                      "execution_gate",
                  ],

                  "execution_evidence": [
                      "apply_execution_result",
                      "executor_write_manifest",
                      "repository_changes",
                      "execution_commit_manifest",
                      "execution_git_commit_result",
                      "execution_pull_request_candidate",
                      "execution_pull_request_result",
                      "execution_provenance",
                  ],
              },

              "flow": [
                  "runtime_auditor_report",

                  "execution_plan",
                  "database_mutation_plan",

                  "persistence_contract",

                  "execution_plan_fingerprint",

                  "plan_audit_evidence",

                  "human_execution_approval",

                  "approval_artifact_matching",

                  "execution_gate",

                  "apply_execution_result",

                  "executor_write_manifest",

                  "repository_changes",

                  "execution_commit_manifest",

                  "execution_git_commit_result",

                  "execution_pull_request_candidate",

                  "execution_pull_request_result",

                  "execution_provenance",

                  "post_audit",
              ],
          }

          Path(
              "artifacts/execution_lineage.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY
          
      ############################################################
      # Verify Executor Provenance Artifacts
      ############################################################

      - name: Verify Executor Provenance Artifacts
        if: always()
        shell: bash
        run: |
          echo "Checking execution provenance artifacts..."

          missing="false"

          for path in \
            artifacts/apply_execution_result.json \
            artifacts/executor_write_manifest.json \
            artifacts/execution_provenance.json
          do
            if [ ! -s "$path" ]; then
              echo "::warning::Missing or empty artifact: $path"
              missing="true"
            else
              echo "Found artifact: $path"
            fi
          done

          if [ "$missing" = "true" ]; then
            echo "::warning::One or more executor provenance artifacts are missing or empty."
          fi

          echo "Executor provenance artifact check completed."      

      ############################################################
      # Automatic Post-Audit Trigger
      ############################################################

      - name: Automatic Post-Audit Trigger
        if: always()
        run: |
          mkdir -p artifacts

          python - <<'PY'
          import json
          from pathlib import Path

          apply_result_file = Path(
              "artifacts/apply_execution_result.json"
          )

          execution_performed = False
          post_audit_required = True

          if apply_result_file.exists():
              try:
                  apply_result = json.loads(
                      apply_result_file.read_text(
                          encoding="utf-8",
                      )
                  )

                  execution_performed = bool(
                      apply_result.get(
                          "execution_performed",
                          False,
                      )
                  )

                  post_audit_required = bool(
                      apply_result.get(
                          "post_audit_required",
                          True,
                      )
                  )

              except Exception:
                  execution_performed = False
                  post_audit_required = True

          payload = {
              "schema":
                  "rga.post_audit_trigger.v1.1",

              "audit_mode":
                  "post_audit",

              "required":
                  post_audit_required,

              "execution_performed":
                  execution_performed,

              "trigger_source":
                  "Bot #2 execution",

              "recommended_workflow":
                  "RGA Runtime Auditor",

              "governance_note": (
                  "Post-audit should be run by Bot #1 after any "
                  "successful Bot #2 execution."
              )
          }

          Path(
              "artifacts/post_audit_trigger.json"
          ).write_text(
              json.dumps(
                  payload,
                  indent=2,
                  ensure_ascii=False,
              ),
              encoding="utf-8",
          )
          PY

      ############################################################
      # Rollback Evidence
      ############################################################

      - name: Rollback Evidence
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          payload = {
              "schema": "rga.rollback_evidence.v1.0",
              "rollback_available": True,
              "strategy": "restore generated executor artifacts",
          }
          Path("artifacts/rollback_evidence.json").write_text(
              json.dumps(payload, indent=2),
              encoding="utf-8",
          )
          PY
          
      ############################################################
      # Write Lifecycle Event Contract
      ############################################################

      - name: Write Lifecycle Event Contract
        shell: bash
        env:
          EXECUTOR_MODE: ${{ inputs.executor_mode }}
          AUDIT_SESSION_ID: ${{ inputs.audit_session_id }}
        run: |
          set -euo pipefail

          mkdir -p artifacts

          EVENT_STAGE=""
          NEXT_STAGE=""
          MODE="${EXECUTOR_MODE:-dry_run_execute}"

          case "$MODE" in

            plan)
              EVENT_STAGE="plan"
              NEXT_STAGE="plan_audit"
              ;;

            execute)
              EVENT_STAGE="execute"
              NEXT_STAGE="post_audit"
              ;;

            dry_run_execute)
              EVENT_STAGE="execute"
              NEXT_STAGE="post_audit"
              ;;

            *)
              echo "Unsupported EXECUTOR_MODE: $MODE" >&2
              exit 1
              ;;
          esac

          jq -n \
            --arg session "${AUDIT_SESSION_ID}" \
            --arg mode "${MODE}" \
            --arg completed "${EVENT_STAGE}" \
            --arg next "${NEXT_STAGE}" \
            '{
              schema: "rga.lifecycle_event.v1.0",
              producer: "maintenance_executor",
              audit_session_id: $session,
              executor_mode: $mode,
              completed_stage: $completed,
              next_stage: $next,
              dispatch_required: true,
              event_contract: {
                completed_stage_declared: true,
                next_stage_declared: true,
                artifact_guessing_required: false,
                runner_must_infer_stage: false
              }
            }' > artifacts/lifecycle_event.json

          echo "Lifecycle event contract written:"
          cat artifacts/lifecycle_event.json

          
      ############################################################
      # Upload Lifecycle Event
      #
      # Purpose:
      #
      #   Publishes lifecycle_event.json as the canonical
      #   stage-transition contract for the RGA Lifecycle Runner.
      #
      # Produced by:
      #
      #   Bot #2 Maintenance Executor
      #
      # Expected completed_stage values:
      #
      #   plan
      #   execute
      #
      # Optional testing mode:
      #
      #   dry_run_execute
      #
      # Canonical output:
      #
      #   artifacts/lifecycle_event.json
      ############################################################

      - name: Upload Lifecycle Event
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rga-lifecycle-event
          path: artifacts/lifecycle_event.json
          if-no-files-found: warn 

      ############################################################
      # Write Lifecycle Event Backhaul
      #
      # Bot #2 Maintenance Executor
      #
      # Purpose:
      #
      #   - Publishes lifecycle continuation evidence.
      #   - Does NOT dispatch the Lifecycle Runner.
      #   - Allows Lifecycle Runner to consume chronological state
      #     via workflow_run artifact download.
      #
      # Canonical output:
      #
      #   artifacts/lifecycle_backhaul.json
      ############################################################

      - name: Write Lifecycle Event Backhaul
        shell: bash
        env:
          EXECUTOR_MODE: ${{ inputs.executor_mode }}
          AUDIT_SESSION_ID: ${{ inputs.audit_session_id }}
        run: |
          set -euo pipefail

          mkdir -p artifacts

          EVENT_STAGE=""
          NEXT_STAGE=""
          DISPATCH_REQUIRED="true"
          LIFECYCLE_STATUS="running"

          case "${EXECUTOR_MODE}" in

            plan)
              EVENT_STAGE="plan"
              NEXT_STAGE="plan_audit"
              ;;

            execute)
              EVENT_STAGE="execute"
              NEXT_STAGE="post_audit"
              ;;

            dry_run_execute)
              EVENT_STAGE="execute"
              NEXT_STAGE="post_audit"
              ;;

            *)
              echo "Unsupported EXECUTOR_MODE: ${EXECUTOR_MODE}" >&2
              exit 1
              ;;

          esac

          jq -n \
            --arg schema "rga.lifecycle_backhaul.v1.0" \
            --arg producer "maintenance_executor" \
            --arg executor_mode "${EXECUTOR_MODE}" \
            --arg session "${AUDIT_SESSION_ID}" \
            --arg completed "${EVENT_STAGE}" \
            --arg next "${NEXT_STAGE}" \
            --arg status "${LIFECYCLE_STATUS}" \
            --argjson dispatch "${DISPATCH_REQUIRED}" \
            '{
              schema: $schema,

              producer: $producer,

              audit_session_id: $session,

              executor_mode: $executor_mode,

              completed_stage: $completed,

              next_stage:
                (
                  if $next == ""
                  then null
                  else $next
                  end
                ),

              lifecycle_status: $status,

              dispatch_required: $dispatch,

              backhaul_contract: {
                bot_dispatches_runner: false,
                runner_must_resume_from_workflow_run: true,
                artifact_guessing_required: false,
                chronological_order_required: true
              },

              governance: {
                bot_role: "planner_executor",
                orchestrator_role: "lifecycle_runner",
                approval_authority: "human",
                completed_phases_remain_immutable: true
              }
            }' \
            > artifacts/lifecycle_backhaul.json

          echo "Lifecycle backhaul written:"
          cat artifacts/lifecycle_backhaul.json  

      ############################################################
      # Upload Lifecycle Event Backhaul
      ############################################################

      - name: Upload Lifecycle Event Backhaul
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rga-lifecycle-backhaul
          path: artifacts/lifecycle_backhaul.json
          if-no-files-found: warn
          retention-days: 30          

      ############################################################
      # Upload Execution Plan Bundle
      #
      # Plan-stage proposal output produced by Bot #2.
      #
      # Primary consumers:
      #
      #   - Runtime Auditor plan_audit
      #   - Human review
      #   - Later dry-run / execute stages
      #
      # execution_plan.json and execution_plan_fingerprint.json
      # are immutable inputs after plan-audit and approval.
      ############################################################

      - name: Upload Execution Plan Bundle
        if: ${{ always() && inputs.executor_mode == 'plan' }}
        uses: actions/upload-artifact@v4
        with:
          name: rga-execution-plan
          path: |
            artifacts/execution_plan.json
            artifacts/execution_plan_fingerprint.json

            artifacts/runtime_executor_report.json
            artifacts/runtime_executor_report.md
            artifacts/runtime_executor_stdout.txt
            artifacts/runtime_executor_stderr.txt
            artifacts/runtime_executor_stderr_summary.json
            artifacts/runtime_executor_exit_code.txt
          if-no-files-found: warn

      ############################################################
      # Stage 2
      # Upload Database Mutation Plan
      #
      # Plan-stage database mutation proposal.
      #
      # This artifact does not authorize database mutation.
      #
      # Primary consumers:
      #
      #   - Runtime Auditor plan_audit
      #   - Persistence contract generation
      #   - Human review
      ############################################################

      - name: Upload Database Mutation Plan
        if: ${{ always() && inputs.executor_mode == 'plan' }}
        uses: actions/upload-artifact@v4
        with:
          name: rga-database-mutation-plan
          path: |
            artifacts/database_mutation_plan.json
          if-no-files-found: warn

      ############################################################
      # Stage 4
      # Upload Persistence Contract
      #
      # Plan-stage governance contract derived from:
      #
      #   artifacts/database_mutation_plan.json
      #
      # This artifact constrains persistence behavior and does not
      # grant execution authority to Bot #2.
      #
      # Primary consumers:
      #
      #   - Runtime Auditor plan_audit
      #   - Human review
      #   - Execute-stage provenance
      #   - Deployment Governance Gate
      ############################################################

      - name: Upload Persistence Contract
        if: ${{ always() && inputs.executor_mode == 'plan' }}
        uses: actions/upload-artifact@v4
        with:
          name: rga-persistence-contract
          path: |
            artifacts/persistence_contract.json
          if-no-files-found: warn

      ############################################################
      # Upload Plan Audit Input Bundle
      #
      # Canonical proposal set reviewed by Runtime Auditor.
      #
      # The bundle intentionally contains only plan-stage contracts.
      #
      # Execute-stage artifacts such as:
      #
      #   approval_artifact_matching.json
      #   execution_plan_input_contract.json
      #
      # do not belong in this bundle because they are generated
      # after plan publication.
      ############################################################

      - name: Upload Plan Audit Input Bundle
        if: ${{ always() && inputs.executor_mode == 'plan' }}
        uses: actions/upload-artifact@v4
        with:
          name: rga-plan-audit-inputs
          path: |
            artifacts/execution_plan.json
            artifacts/execution_plan_fingerprint.json
            artifacts/database_mutation_plan.json
            artifacts/persistence_contract.json
          if-no-files-found: warn

      ############################################################
      # Upload Dry Run Evidence
      #
      # Dry-run mode consumes the preserved plan-stage artifacts.
      #
      # It must not overwrite:
      #
      #   execution_plan.json
      #   database_mutation_plan.json
      #   persistence_contract.json
      #   execution_plan_fingerprint.json
      #
      # runtime_executor_output.json is the mode-specific executor
      # output for dry_run_execute.
      ############################################################

      - name: Upload Dry Run Evidence
        if: ${{ always() && inputs.executor_mode == 'dry_run_execute' }}
        uses: actions/upload-artifact@v4
        with:
          name: rga-dry-run-evidence
          path: |
            artifacts/execution_plan.json
            artifacts/execution_plan_fingerprint.json
            artifacts/database_mutation_plan.json
            artifacts/persistence_contract.json

            artifacts/execution_plan_input_contract.json
            artifacts/runtime_executor_output.json

            artifacts/runtime_executor_report.json
            artifacts/runtime_executor_report.md
            artifacts/runtime_executor_stdout.txt
            artifacts/runtime_executor_stderr.txt
            artifacts/runtime_executor_stderr_summary.json
            artifacts/runtime_executor_exit_code.txt
          if-no-files-found: warn

      ############################################################
      # Upload Execute Evidence
      #
      # Complete execute-stage evidence bundle.
      #
      # The bundle preserves the entire approved-plan chain:
      #
      #   proposal contracts
      #     ->
      #   integrity contract
      #     ->
      #   plan audit
      #     ->
      #   human approval
      #     ->
      #   approval matching
      #     ->
      #   execution gate
      #     ->
      #   executor evidence
      #     ->
      #   repository / commit / PR evidence
      #     ->
      #   execution provenance
      #
      # Including the approval and input contracts is necessary to
      # diagnose approved-plan identity without reconstructing state
      # from separate workflow runs.
      ############################################################

      - name: Upload Execute Evidence
        if: ${{ always() && inputs.executor_mode == 'execute' }}
        uses: actions/upload-artifact@v4
        with:
          name: rga-execute-evidence
          path: |
            artifacts/execution_plan.json
            artifacts/execution_plan_fingerprint.json
            artifacts/database_mutation_plan.json
            artifacts/persistence_contract.json

            artifacts/plan_audit_evidence.json
            artifacts/human_execution_approval.json
            artifacts/execution_plan_input_contract.json
            artifacts/approval_artifact_matching.json
            artifacts/approval_audit.json
            artifacts/execution_gate.json
            artifacts/execution_scope_audit.json
            artifacts/execution_policy_audit.json

            artifacts/runtime_executor_output.json
            artifacts/runtime_executor_report.json
            artifacts/runtime_executor_report.md
            artifacts/runtime_executor_stdout.txt
            artifacts/runtime_executor_stderr.txt
            artifacts/runtime_executor_stderr_summary.json
            artifacts/runtime_executor_exit_code.txt

            artifacts/apply_execution_result.json
            artifacts/executor_write_manifest.json
            artifacts/execution_provenance.json

            artifacts/repository_changes.json
            artifacts/execution_commit_manifest.json
            artifacts/execution_git_commit_result.json

            artifacts/execution_pull_request_candidate.json
            artifacts/execution_pull_request_result.json
            artifacts/execution_pull_request_url.txt
            artifacts/execution_pull_request_stderr.txt

            artifacts/execution_manifest.txt
            artifacts/execution_lineage.json
          if-no-files-found: warn

      ############################################################
      # Upload Execution Provenance Bundle
      #
      # Focused provenance subset for:
      #
      #   - Runtime Auditor post_audit
      #   - Deployment Governance Gate
      #   - Human governance review
      #
      # This bundle includes plan identity and approval identity,
      # not only executor write evidence.
      ############################################################

      - name: Upload Execution Provenance Bundle
        if: ${{ always() && inputs.executor_mode == 'execute' }}
        uses: actions/upload-artifact@v4
        with:
          name: rga-execution-provenance
          path: |
            artifacts/execution_plan.json
            artifacts/execution_plan_fingerprint.json
            artifacts/database_mutation_plan.json
            artifacts/persistence_contract.json

            artifacts/plan_audit_evidence.json
            artifacts/human_execution_approval.json
            artifacts/execution_plan_input_contract.json
            artifacts/approval_artifact_matching.json
            artifacts/execution_gate.json

            artifacts/apply_execution_result.json
            artifacts/executor_write_manifest.json
            artifacts/execution_provenance.json

            artifacts/repository_changes.json
            artifacts/execution_commit_manifest.json
            artifacts/execution_git_commit_result.json
            artifacts/execution_pull_request_candidate.json
            artifacts/execution_pull_request_result.json

            artifacts/execution_manifest.txt
            artifacts/execution_lineage.json
          if-no-files-found: warn     
