# Model Registry

Defines how trained models are stored and referenced.

Each model entry should include:
- model_id
- model_version
- training_dataset_id
- promotion_status
- rollback_target

Registry is append-only.
